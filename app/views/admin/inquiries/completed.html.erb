<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<h4>対応済みの問合せ一覧</h4>
		<table class="table">
			<tbody>
				<% @inquiries.each do |inquiry| %>
				<tr>
					<td><%= inquiry.user.name %>様</td>
					<td><%= inquiry.title %></td>
					<td><%= link_to "詳細へ", admin_inquiry_path(inquiry) %></td>
				</tr>
				<% end %>
			</tbody>
		</table>

		<%= link_to "未対応のお問い合わせ", admin_inquiries_path %>
	</div>
</div>